<h2><%= @list.name %></h2>

<%= link_to "Add a bookmark", new_list_bookmark_path(@list, @bookmark) %>


<%= link_to "Delete a list",
  list_path(@list),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
